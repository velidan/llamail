Below are summaries of different sections of a long email. Combine them into one cohesive analysis.

From: {{ from_name or "Unknown" }} <{{ from_address }}>
Subject: {{ subject or "(no subject)" }}
Date: {{ date }}

--- SECTION SUMMARIES ---
{% for chunk in chunk_summaries %}
Part {{ loop.index }} of {{ chunk_summaries | length }}: {{ chunk }}
{% endfor %}
--- END ---

Return this exact JSON structure, nothing else:
{
    "summary": "2-3 sentence cohesive summary of the ENTIRE email",
    "category": "work | personal | newsletter | notification | finance | social | spam | other",
    "priority": "high | medium | low",
    "sentiment": "positive | negative | neutral | urgent",
    "action_required": true or false,
    "action_items": ["action item 1", "action item 2"] or [],
    "key_people": ["Person name"] or []  
}